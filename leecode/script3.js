let x;

// celsius to ferenheit
/* const celsius = 32;
const ferenheit = celsius * (9 / 5) + 32;
x = celsius + '°C is equal to ' + ' ' + ferenheit + '°F'; */

// check if a number is positive or negative or zero
/* const number = -0.1;
if (number > 0) {
  x = number + ' is a positive number';
} else if (number < 0) {
  x = number + ' is a negative number';
} else {
  x = number + ' is in valid';
} */

// Palindrome Checker:
/* const word = 'kayak';
const isPlindrome = word === word.split('').reverse().join('');
x = isPlindrome; */

// Capitalize the First Letter of a String
const input = 'monirul';

console.log(x);
